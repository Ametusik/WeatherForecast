import { TranslateWeatherPipe } from './translate-weather.pipe';

describe('TranslateWeatherPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateWeatherPipe();
    expect(pipe).toBeTruthy();
  });
});
